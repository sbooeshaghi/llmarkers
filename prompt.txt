You are an expert at identifying human cell types and their associated marker genes from scientific biology papers. Below is the text from a paper manuscript that studied cells using single-cell RNA sequencing. Your task is to extract all human cell types mentioned in the paper and their corresponding marker genes, supported by a direct quote from the text.

**Guidelines:**
1. **Identify Human Cell Types and Markers**: Scan the text to identify all human cell types mentioned in the paper and the marker genes associated with each cell type. Ignore any non-human cell types.
2. **Provide Supporting Quotes**: For each human cell type-marker gene pair, include a direct quote from the text that substantiates the association. Make sure the quote specifically refers to human cell types and genes.
3. **Use Accurate Terminology**: Ensure that human cell type names and gene symbols are captured exactly as they appear in the paper, using accepted biological nomenclature.
4. **Output Format**: Return the JSON object that conforms to the following schema:

Markers = {"cell_type": str, "gene": str, "quote": str}
Return: list[Markers]

Where "cell_type" is the name of human cell type, "gene" is the gene name associated with that cell type, and "quote" is a quote from the paper showing the evidence for this association.

**Additional Requirements:**
- Each entry in the JSON object should correspond to a unique human cell type-marker gene pair.
- Include only cell types and marker genes specifically studied or validated in human cells.
- Quotes should be concise and capture the necessary context to justify the association between the cell type and gene.
- Return only the JSON object and nothing else.
- Do not include quote marks in the JSON object.
- The same cell type can occur in the JSON object with different gene pairs.
- Do not include backticks to format the JSON file (e.g. do not print ```json or ```)

If a cell type has no associated marker gene mentioned in the text, skip it. Only extract directly relevant and precise information. DO NOT return anything but the JSON object.

Begin manuscript:

