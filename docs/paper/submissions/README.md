# Submission Packages

Use one subdirectory per submitted venue or target.

Recommended layout:

```text
submissions/
  <venue-or-target>/
    cover_letter.tex
    response_to_reviewers.md
    portal_abstract.txt
    notes.md
```

Keep target-specific files here instead of mixing them into the active bioRxiv manuscript workspace.
